import React from "react";
import "../styles.css"; // Import CSS for consistent styling

const Home = () => (
  <div className="container">
    <h1>Welcome to E-Commerce Microservices Project</h1>
    <p>Use the navigation bar to explore Products, Orders, and manage Users.</p>
    <p>This is a microservices-based application using Spring Boot, Eureka, and React.</p>
  </div>
);

export default Home;